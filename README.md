## 기능 목록

### 입력
- 콘솔에서 덧셈할 숫자 문자열을 입력받는다
- 사용자가 입력한 문자열의 각문자가 유효한 문자인지 검증한다

### 구분자 처리
- 기본 구분자(쉼표, 콜론)를 사용하여 숫자를 분리한다
- 커스텀 구분자가 있다면 구분자 목록에 추가한다(`//구분자\n숫자` 형식)

### 계산
- 구분자로 분리된 숫자들의 총합을 계산한다
- 계산 결과를 출력한다